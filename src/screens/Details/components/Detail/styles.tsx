import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  label: { fontWeight: 'bold', textTransform: 'capitalize' },
  detail: { color: 'white', fontSize: 20 },
});

export default styles;
